<%= form_for @static_page, url: @url do |f| %>
  <div class="form-group">
    <%= f.label 'Título' %><br>
    <%= f.text_field :name, class: 'form-control', disabled: !@static_page.editable %>
  </div>
  
  <div class="form-group">
    <%= f.label :background_image,'Imagen de Fondo' %>
    <span> <%= "(#{@static_page.try(:background_image_file_name)})" %></span>
    <%= f.file_field  :background_image, class: 'form-control' %>
    <div class="field checkbox" style="margin-left:15px">
      <label>
        <%= check_box_tag :default_bg_image,'1', false %>
        <%= Spree.t('default_bg_image') %>
      </label>
    </div>
  </div>

  <div class="form-group">
    <%= f.label 'Contenido' %><br>
    <%= f.text_area :content, {rows: 20, cols: 100} %>
  </div>
  <div class="form-group">
    <%= f.submit 'Guardar', class: 'btn btn-primary' %>
  </div>
<% end %>